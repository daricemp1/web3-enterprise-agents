# Vertex AI Agent Engine deployment config for the dev environment.
# Values are placeholders until a real GCP dev project is provisioned (see later plan).
environment: dev
gcp_project_id: REPLACE_WITH_DEV_PROJECT_ID
region: us-central1
service_account: REPLACE_WITH_DEV_SERVICE_ACCOUNT_EMAIL
staging_bucket: REPLACE_WITH_DEV_STAGING_BUCKET
bigquery:
  compute_project_id: REPLACE_WITH_DEV_PROJECT_ID
  location: US
agent_engine_resource_name: null
agentspace:
  app_id: REPLACE_WITH_AGENTSPACE_APP_ID
  registered_agent_id: null
