# Logical agent template

`logical_agent/` is a scaffold template, not a runnable agent. It is copied and token-substituted
by `_shared/scripts/scaffold_logical_agent.py` — do not run `adk run` directly against it.
See docs/superpowers/specs/2026-07-25-retail-merchandising-adk-agents-design.md section 4.
